/*stack容器  不允许遍历行为 无迭代器*/
/*
	(1)构造
		stack<type>s;
		stack(const stack &stk)  拷贝构造
		stack<int>stack1=stack2  赋值
	(2)数据存取
		stk.push(elem)  压入数据
		stk.pop()  弹出数据
		stk.top()  返回栈顶元素
	(3)大小操作
		stk.empty()  判空
		stk.size()  返回栈大小 
*/
#include<iostream>
#include<stack> 
using namespace std;
int main(){
	return 0;
}
