/*queue 容器  无迭代器 不支持遍历*/
/*
	(1)构造
		queue<type>q
		queue<type>q2=q1
		queue(const queue &que) 拷贝构造
	(2)数据操作
		que.push(elem)
		que.pop()
		que.back() 返回最后一个元素 
		que.front() 返回第一个元素
	(3)大小操作 
		que.empty()
		que.size() 
*/
#include<iostream>
#include<queue>
using namespace std;
int main(){
	return 0;
} 
